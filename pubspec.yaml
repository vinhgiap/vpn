name: securevpn
description: A new Flutter project.
publish_to: "none"

version: 1.0.2+3

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  google_fonts: ^5.0.0
  flutter_carousel_slider: ^1.1.0
  shared_preferences: ^2.1.1
  provider: ^6.0.5
  http: ^1.0.0
  openvpn_flutter_update: ^1.2.2+1
  flutter_math_fork: ^0.7.1
  webview_flutter: ^4.2.2
  firebase_core: ^2.15.0


dev_dependencies:
  chewie_audio: ^1.5.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assests/image/
    - assests/country/
